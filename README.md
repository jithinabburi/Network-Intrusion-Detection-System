# Network-Intrusion-Detection-System
Network Intrusion Detection System (NIDS) is a real-time cybersecurity project built using Python, Scapy, and Machine Learning. It captures live network traffic, extracts packet features, and uses a Random Forest model to classify traffic as normal or attack, with rule-based alerts and a Streamlit dashboard.
